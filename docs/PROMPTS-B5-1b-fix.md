# PROMPTS — B5-1b-fix: 3 бага доски (прогресс/клик/высота)

> Все три — в Доска-виде (/planner). Без миграции.
> 1. Шапка прогресса показывает только НЕДЕЛЬНЫЙ план; дневной (WeeklyPlan.date != null) не отображается.
> 2. Клик по разным карточкам открывает одну и ту же отгрузку.
> 3. Высота колонки ограничена → при многих карточках они поджимаются (013242.png) вместо скролла колонки.

---

## ПРОМПТ — B5-1b-fix (Claude Code)

```text
Задача B5-1b-fix (VSMS): три бага Доска-вида (/planner). Без миграции. Перед кодом — лоадер доски
(getBoardWeek), Доска-вид и карточка, server/plan/actions.ts (getPlanWeek — умеет оба режима плана).

1) ПРОГРЕСС: дневной план не виден в шапке.
   WeeklyPlan.date: null = недельная цель (1 строка на неделю/культуру); задана = дневные строки (BR-20,
   не смешиваются). Сейчас прогресс берёт только недельные строки → при дневном плане target пустой.
   Фикс: target по культуре за неделю = НЕДЕЛЬНАЯ строка (date null) ИЛИ Σ дневных строк (date != null) той
   же (iso_year, iso_week, culture). ПЕРЕИСПОЛЬЗОВАТЬ агрегацию getPlanWeek (она уже сводит оба режима) —
   не считать заново. Полоса прогресса показывает все культуры недели с целью в любом режиме.

2) КЛИК ПО КАРТОЧКЕ открывает одну и ту же отгрузку (stale binding).
   Причина почти наверняка: общий стейт диалога/замыкание захватывает не тот shipmentId (напр. id из
   внешней области, индекс вместо id, или один selectedId, выставляемый константой). Фикс: каждый клик
   открывает диалог ДЛЯ СВОЕГО shipment.id — onClick передаёт id конкретной карточки; диалог рендерится по
   выбранному id; ключи списка уникальны (key={shipment.id}); никаких захватов последней итерации.
   Проверить также карточку-машину (открывает свой Shipment).

3) ВЫСОТА КОЛОНКИ: карточки поджимаются вместо скролла.
   Фикс: карточки НЕ сжимаются — .card { flex: 0 0 auto } (flex-shrink: 0); контейнер колонки имеет
   ОГРАНИЧЕННУЮ высоту (доска = высота вьюпорта минус шапка/прогресс), .col-body { overflow-y: auto } реально
   скроллится. Поджимается страница/доска, а колонка прокручивается — карточки сохраняют контентную высоту,
   ничего не обрезается. Проверить, что у .card/.mcard нет min-height:0 в flex-контексте, дающего сжатие.

ОГРАНИЧЕНИЯ:
- Только Доска-вид (лоадер прогресса + карточка/клик + верстка колонки). Схему/миграции/план-вид/ленту — не трогать.
- Сборка/дефицит — отдельные срезы (заглушки не ломать). Доки/TASKS — не трогать. lucide/tabular-nums.

БД ТЕСТОВАЯ: проверки через UI/seed.

ПРОВЕРКА (через UI, показать):
- ДНЕВНОЙ план: задать цели по дням для культуры → в шапке доски прогресс культуры = Σ дневных целей (виден).
  Недельный план (date null) — по-прежнему виден. Несколько культур с целями — все в полосе.
- КЛИК: клик по разным карточкам открывает РАЗНЫЕ отгрузки (одно-фермерская и машина — каждая свою).
- ВЫСОТА: день с 6+ карточками → колонка скроллится вертикально, карточки НЕ поджаты/не обрезаны; неделя
  по-прежнему без горизонтального скролла.
- регресс: dnd/валидация/«+»/сайдбар (B5-1b) и план-вид работают.
В конце предложи git-коммит одной строкой.
```

### Чеклист ревью B5-1b-fix
1. Прогресс: target культуры = недельная ИЛИ Σ дневных (через getPlanWeek); дневной план виден; недельный не сломан.
2. Клик: каждая карточка открывает свой Shipment (id передаётся верно, ключи уникальны, нет stale-замыкания); машина — свой.
3. Колонка: .card flex:0 0 auto (не сжимается); .col-body overflow-y:auto скроллится; высота доски = вьюпорт−шапка; ничего не обрезается.
4. Горизонтального скролла недели нет; dnd/валидация/«+»/сайдбар — регресс зелёный; схема/план-вид/лента не тронуты.
