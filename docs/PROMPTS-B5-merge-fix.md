# PROMPTS — B5-merge-fix: 4 UI-правки доски

> Косметика/UX после B5-merge. Без миграции, без серверной логики.
> 1. Порядок переключателя → «Доска | План» (Доска первой).
> 2. Сайдбар не сворачивается при ПЕРВОМ заходе на /planner (доска) — только после План↔Доска.
> 3. Кнопка «Собрать машину» не по макету — перекрасить под прототип (содержимое то же).
> 4. Тоггл «Выбрать/Готово» съедает строку между прогрессом и карточками — перенести в полосу прогресса справа.

---

## ПРОМПТ — B5-merge-fix (Claude Code)

```text
Задача B5-merge-fix (VSMS): 4 UI-правки Доска/Планировщик. Без миграции, без изменения серверной логики.
Перед кодом — Доска-вид, полоса прогресса, тулбар-шелл /planner, сайдбар/лэйаут, прототип board-b5-v2.html.

1) Переключатель видов /planner: порядок «Доска | План» (Доска ПЕРВАЯ). Дефолт уже board (B5-merge) — оставить.

2) Сайдбар не сворачивается на ПЕРВОМ рендере /planner с view=board (сейчас сворачивается только после
   переключения План↔Доска). Причина — эффект/логика срабатывает на СМЕНУ вида, а не на маунт. Фикс: при
   маунте/заходе, если активный вид = board, сразу свернуть сайдбар; вне board (план/другие маршруты) —
   развернуть. Ручной тоггл в пределах доски УВАЖАТЬ (не схлопывать повторно до смены вида/маршрута).
   Без localStorage.

3) Кнопка «Собрать машину (N)» (панель сборки) — перекрасить под прототип board-b5-v2.html (.assemble-bar/
   кнопка сборки): те же цвета/заливка/типографика, что в макете. СОДЕРЖИМОЕ и поведение не менять — только стиль.

4) Тоггл режима выбора «Выбрать/Готово» сейчас в строке МЕЖДУ полосой прогресса и карточками (съедает место).
   Перенести его в ПОЛОСУ ПРОГРЕССА СПРАВА — компактный кластер действий, выровненный по правому краю
   (там пустое место). Строку-прокладку убрать. Кластер сделать расширяемым (позже рядом встанет
   «+ Целая машина» из B5-bulk — оставить место/контейнер под второе действие, но саму кнопку сейчас НЕ добавлять).
   Поведение тоггла (вкл/выкл режим выбора, «Готово» сбрасывает выбор) не менять.

ОГРАНИЧЕНИЯ:
- Только UI Доска-вида/полосы прогресса/сайдбара/переключателя. Сервер/схему/merge-логику — не трогать.
- Без новых цветов вне DESIGN-SYSTEM/прототипа. lucide/tabular-nums. Доки/TASKS — не трогать.

ПРОВЕРКА (через UI, показать):
- переключатель: «Доска | План», Доска первой и по умолчанию.
- первый заход на /planner → сайдбар свёрнут (без переключения видов); уход на «План»/в «Ленту» → развёрнут;
  ручной разворот на доске не схлопывается сам.
- кнопка «Собрать машину» визуально совпадает с прототипом.
- тоггл «Выбрать/Готово» в полосе прогресса справа; строки-прокладки нет; режим выбора/сборка работают.
В конце предложи git-коммит одной строкой.
```

### Чеклист ревью B5-merge-fix
1. Порядок «Доска | План», Доска первой/дефолт.
2. Сайдбар свёрнут на первом заходе (view=board), развёрнут вне; ручной тоггл уважается; без localStorage.
3. «Собрать машину» по прототипу (только стиль).
4. Тоггл «Выбрать/Готово» в полосе прогресса справа; прокладка убрана; кластер с местом под второе действие; поведение не сломано.
5. Только UI; сервер/схема/merge не тронуты.
