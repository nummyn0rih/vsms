# CONTEXT-HANDOFF — VSMS

> Цель: за 5–10 минут восстановить контекст в новом чате. Не пересказ проекта — только то, что нужно, чтобы
> продолжить без повторного проектирования уже решённого. HEAD reflog на момент сборки: `4aa63a6`.

---

# ENVIRONMENT / MACHINE SETUP (перенос WSL → Mac — удалить блок, когда Mac зелёный)

> Транзитное знание миграции окружения, НЕ часть project state. Как проект поднимется на Mac и пройдут
> tsc/build — этот блок можно снести.

- **Перенести руками — только `.env`** (gitignored: `.env*` кроме `.env.example`; git его не тащит).
  Ключи: `DATABASE_URL`, `DIRECT_URL`, `AUTH_SECRET`, `AUTH_URL`, `SEED_ADMIN_LOGIN/PASSWORD`. Шаблон — `.env.example`.
- **НЕ переносить (регенерится, чужая копия вредна):** `next-env.d.ts` (авто-типы Next), `*.tsbuildinfo`
  (инкрементальный кэш tsc → чужой кэш даёт ложное «без изменений»). Осиротевший `tsconfig.diag.tsbuildinfo`
  (`tsconfig.diag.json` уже нет) — просто мусор, удалить.
- **Шаги на Mac после clone:** `npm ci` → **`npx prisma generate`** (генератор пишет в gitignored
  `lib/generated/prisma` — без этого tsc падает на импортах Prisma-типов) → `npm run dev` / `npm run build`;
  опц. `npx prisma migrate status`.
- **БД — облачный Neon, НЕ локальный PG.** Тот же `DATABASE_URL` → Mac работает с той же базой; ре-сид и
  миграция данных не нужны, `.env` достаточно.
- **Пересмотреть на Mac:** `prisma.config.ts` гонит миграции через **pooled** `DATABASE_URL` — это
  WSL-компромисс («direct-эндпоинт недоступен из сети WSL»). На Mac direct-коннект, вероятно, доступен →
  вернуть `env("DIRECT_URL")` для `migrate`/`studio`.
- **`NODE_OPTIONS=--dns-result-order=ipv4first`** во всех npm-скриптах (`package.json`) — обход IPv6↔Neon в
  WSL. На Mac, вероятно, лишний; безвреден, можно оставить.
- **Версии на WSL:** Node v24.15.0, npm 11.12.1 (`.nvmrc` нет — свериться вручную).
- **Doc-drift к исправлению:** `CLAUDE.md` пишет «`directUrl` в `schema.prisma`» — устарело (Prisma 7: URL
  живёт в `prisma.config.ts`, в `datasource` только `provider`). Поправить при случае.

---

# PROJECT STATE

- **Проект:** VSMS — внутренняя веб-система учёта поставок овощного сырья на консервный завод (замена Excel/Sheets).
  Solo-разработка через AI. Стек: Next.js (App Router) + TS · PostgreSQL (Neon) + Prisma · Auth.js v5 · Tailwind +
  shadcn · @dnd-kit · **Recharts** · **SheetJS (xlsx)**.
- **Версии стека:** Next **16.2.7** (⚠ middleware-конвенция = корневой **`proxy.ts`**, НЕ `middleware.ts`) ·
  React 19.2.4 · Prisma **7.8** (+`@prisma/adapter-pg`; URL в `prisma.config.ts`, не в schema) · Auth.js
  5.0.0-beta.31 · zod 4 · Tailwind 4 · Recharts 3 · SheetJS 0.18.
- **Процесс (двухуровневый, заведён в этой сессии):** PM-ассистент (этот контекст) пишет спеки/дизайн-спеки, ведёт
  память; **Claude Code (CLI)** реализует. Артефакты обмена — `docs/prompts/PROMPTS-*.md` (код-спеки),
  `docs/prompts/DESIGN-*.md` (брифы для Claude Design → прототипы в `docs/prototypes/`). Repo-агенты
  `.claude/agents/{invariant-review,test-runner}`. Шаблон и правила — `docs/prompts/_PM-WORKFLOW.md`.
- **Завершённые этапы:** A–E (MVP), D2-ops, D3-2, transfer, B5 (доска), shipment-revert, acceptance auto-date;
  **V1.1** (Сводка/Heatmap · AlertRule-алерты · карточка поставщика · фильтры /materials);
  **F/V2** — аналитический дашборд (Recharts введён) · факт. ср. вес рейса · мобильная v1 (лента/приёмка/водители) ·
  мобильная v2 (Сводка/План) · печатные формы (5 листов) · печать ленты 3-режима + «развернуть все» ·
  фильтры десктопной приёмки · палитра культур (`CULTURE_PALETTE`) · экспорт Excel (Отгрузки/Приёмка/Сводка/План) ·
  аналитика по культуре.
- **Фильтры мобильной приёмки — ЗАКРЫТЫ** (commit `a723804`): bottom-sheet Поставщик·Сырьё·поиск на общем
  `filterBoard`/`boardOptions` (новый `MobileAcceptanceFilterSheet.tsx` + правка `MobileAcceptanceBoard.tsx`),
  без сервера/миграции/новых CSS.
- **Следующий приоритет — ПРОД-ВЫКАТ** (см. `docs/prompts/PROD-DEPLOY.md`): развернуть текущий main в прод,
  завести ОТДЕЛЬНУЮ прод-БД (реальные данные), перейти на dev-ветку для новых фич. Остаток фич-бэклога:
  **календарь** (в последнюю очередь), Excel Аналитики-таблиц, снос deprecated-колонок, сравнение сезонов (этап G).

---

# CURRENT OBJECTIVE

**ПРОД-ВЫКАТ MVP.** Весь функциональный контур закрыт (MVP+V1.1+F/V2, включая фильтры мобильной приёмки).
Задача — вывести текущий `main` в продакшн: отдельная прод-БД с реальными данными (не тестовая-одноразовая),
хостинг, прод-секреты/миграции (`migrate deploy`, не `migrate dev`), бэкапы; после — работа новых фич через
`dev`-ветку с preview-деплоями, merge в `main` = релиз. План и решения — `docs/prompts/PROD-DEPLOY.md`.
Фич-бэклог (календарь и пр.) — ПОСЛЕ стабилизации прода.

---

# SESSION SUMMARY

Огромная сессия: закрыт весь V1.1 и большой пласт F/V2 + пользовательский бэклог. Относительно прошлого handoff
(был на закрытии E) сделано:
- **V1.1:** Сводка/Heatmap (вид на /shipments из `getPlanWeek`) · AlertRule-алерты (панели дефицита + бейдж) ·
  карточка поставщика (`/reference/farmers/[id]`, 4 вкладки) · фильтры /materials (общий `FilterCombo`).
- **F/V2:** аналитический дашборд `/analytics` (`getSeasonAnalytics`, Recharts) · факт. ср. вес рейса
  (`aggregateActualTripWeight`) · мобильная v1 (шелл: app-bar+таб-бар+drawer; лента-карточки; приёмка; водители) ·
  мобильная v2 (read-only Сводка/План) · печатные формы (`app/print/*`: Отгрузки/Приёмка/Сводка/План/Аналитика) ·
  печать ленты в 3 режима + «развернуть все недели» · фильтры десктопной приёмки · палитра культур
  (`CULTURE_PALETTE`, swatch-пикер) · экспорт Excel (SheetJS, `lib/xlsx-export`) · аналитика по культуре
  (`/analytics/culture/[id]`, `getCultureAnalytics`).
- **Процесс/память:** заведён двухуровневый PM↔Claude-Code контур (`_PM-WORKFLOW.md`), repo-агенты, scheduled
  `vsms-memory-hygiene`; прогнан on-demand аудитор согласованности (нашёл только доковый дрейф — вычищен).
- **Обновлённые доки:** этот файл; `TASKS.md` (статусы + бэклог пользователя); `DOMAIN.md` (BR-24 acceptance
  auto-date; §5 «осталось ~N машин» = факт. ср. вес рейса с fallback на `TripWeightNorm`); `PRD.md`/`DESIGN.md`
  (Экран 4/5, мобильная реализована); `DESIGN-SYSTEM.md` (палитра культур). Все спеки — `docs/prompts/PROMPTS-*.md`,
  прототипы — `docs/prototypes/*.html`.
- **Изменённые области кода:** `server/analytics/{dashboard,culture}.ts`, `server/farmers/card.ts`,
  `server/alert-rules/alerts.ts`, `server/acceptance/board.ts` (`computeWeightedBrak`), `app/(app)/analytics/*`,
  `app/(app)/reference/farmers/[id]/*`, `app/print/*`, `components/filters/FilterCombo`, `components/shell/FeedToolbar`,
  мобильные компоненты в `app/(app)/{shipments,acceptance,planner,reference/drivers}`, `lib/{xlsx-export,culture-palette}`.

---

# ARCHITECTURAL DECISIONS
*(зафиксировано; не менять без обсуждения)*

- **Двухуровневый процесс PM↔Claude Code.** Причина: solo-dev с AI; PM держит живую модель и пишет проверяемые
  спеки, Claude Code исполняет в среде с git/тестами. Нельзя менять: PM не редактирует прод-код; фиксы — отдельными
  самодостаточными `PROMPTS-*-FIX.md`; новый экран → дизайн-спека → Claude Design → ревью → код-спека.
- **Балансы/стоимость/принятый вес/выполнение — вычисляются, не хранятся.** Причина: единый источник истины,
  нет рассинхрона. Нельзя: писать закешированные суммы; дублировать формулы агрегации (одна чистая функция на всё).
- **Печать — print-CSS, отдельные bare-роуты `app/print/*`** (НЕ `@media print` поверх интерактивных экранов, НЕ
  серверная PDF-библиотека). Причина: экраны (карточки-леджер, heatmap, Recharts) в чистые A4-таблицы через CSS не
  превращаются; браузерная печать/«Сохранить как PDF» без зависимостей. Нельзя: менять `@media print`-вывод под
  экранные правки; печатать деньги (натуральные единицы). Сводка/План — landscape, остальное portrait.
- **Экспорт Excel — клиентский SheetJS через один хелпер `lib/xlsx-export`.** Причина: фильтры видов клиентские,
  экспорт «что видишь»; одна точка SheetJS. Нельзя: вторая точка использования; числа строками.
- **Палитра культур `CULTURE_PALETTE` (12 цветов) — источник выбора цвета; `Culture.color` остаётся hex.** Причина:
  произвольный hex давал неразличимые/статусные цвета; миграции избежали. Нельзя: ломать off-palette-культуры (их
  цвет — «свой цвет»); запрещать дубли (только предупреждать); хардкодить цвета мимо константы.
- **Аналитика по культуре — отдельный экран `/analytics/culture/[id]`; % брака графиком по неделям; выполнение у
  фермера без контракта = «—».** Причина: профиль просторный и шарабельный; drill-down нужен, чтобы видеть *когда*
  всплеск брака; «—» не путать с «мало принял». Нельзя: деньги-KPI, год-к-году (нет истории).
- **Три базы веса (DOMAIN §1):** тара — плановый (на `sent`); ингредиент — фактический; деньги/принятый — принятый;
  брак — статистика, в выполнение не идёт. Нельзя путать базы.
- **Единый складской движок `StockMovement` (kind), нетто-дисциплина плеч, транзит-сентинелы −1/−2/−3.** Причина:
  один леджер, обратимость, повторный цикл после отката. Нельзя: новое плечо без нетто-гарда; новые сентинелы.
- **Состояние недели/вида/сезона — в URL, не localStorage.** Причина: шарабельность, воспроизводимость.
  (Клиентские фильтры ленты/приёмки — React-Set, сериализуются в URL для печати/экспорта.)

---

# REJECTED ALTERNATIVES
*(обсуждено и отвергнуто; не возвращаться без новых данных)*

- **Печать акта позиции как первую печатную форму** — предлагалось; отвергнуто: пользователю нужны табличные формы
  рабочих ВИДОВ (Отгрузки/Сводка/План/Приёмка/Аналитика), не документ-акт. `DESIGN-ACT-PRINT.md` помечен замещённым.
- **Упрощать Аналитику до числовых таблиц на печати** — отвергнуто: аналитика с графиками нагляднее (единственный
  печатный вид с графикой). Рабочие данные — таблицами, Аналитика — с графиками.
- **`@media print` поверх интерактивных экранов** — отвергнуто: ledger/heatmap/charts не сводятся к чистым таблицам
  CSS-ом. → отдельные print-представления.
- **Route-group `(print)`** — отвергнуто: не даёт URL-сегмента, `(print)/shipments` → `/shipments` (конфликт). →
  обычный сегмент `app/print/*`.
- **Серверная PDF-библиотека (@react-pdf/pdf-lib)** — отвергнуто в пользу print-CSS (без новых зависимостей, «no
  over-engineering»).
- **% брака одним числом + спарклайн (в аналитике культуры)** — отвергнуто: среднее уже в KPI; график по неделям
  показывает *когда* всплеск. Оставлен bar-по-неделям.
- **Весь апп адаптивно (мобильная версия)** — отвергнуто: часть экранов на телефоне малополезна. Мобиле v1 = 3
  полевых экрана; v2 = read-only Сводка/План.
- **Сравнение сезонов год-к-году сейчас** — отложено (не отвергнуто): нет исторических данных, импорт — этап G.
- **Пер-строковая интенсивность heatmap глобальным максимумом** — отвергнуто: слабые культуры бледнеют → по строке
  культуры.

**Структурные (эпоха склада/движка — единственная запись, в DOMAIN/PRD как «отвергнутое» их нет):**
- **Зеркальные ingredient-/transfer-копии плеч** (вместо параметризации единого движка по `kind`+origin+транзит)
  — дублирование сотен строк.
- **`material_shipment_item_id` в леджер** (ради дублей позиций) — over-engineering; решено zod-запретом дублей
  `(farmer, kind, fk)`, дубль-строка = одна строка с суммой.
- **Отдельные таблицы `MaterialTransfer` / `PlannedPickup`** — дублируют движок и «план vs факт»; решено
  расширением существующих сущностей.
- **`partially_arrived` в enum `ShipmentStatus`** — enum общий с продукцией, протечёт; «частично» — производное
  из `MaterialShipmentItem.arrived_at`.
- **Capacity-предупреждение для смешанных машин** — нет максимума фуры; `TripWeightNorm` — вес рейса
  per фермер×культура; хинт только для одно-парных карточек.
- **Двухступенчатый калибр / снимки accepted-brak / один транзит / тара одним движением на `sent`** — ранние
  модели, заменены текущей (одноступенчатый принятый вес, нетто-сторно, сентинелы −1/−2/−3, двухфазное движение).

---

# CURRENT IMPLEMENTATION STATE

**Работает (закрыто, тесты зелёные):** весь MVP (A–E) + контуры поставки/тары/ингредиентов/планирования/переноса/
откатов; V1.1; F/V2 (см. PROJECT STATE). Ключевые агрегаты: `getPlanWeek` (Сводка/План/их печать+Excel),
`getSeasonAnalytics`+`getCultureAnalytics` (дашборд+профиль культуры), `getContractExecution` (карточка/выполнение),
`getAcceptanceBoard`+`computeAcceptedKg`+`computeWeightedBrak` (приёмка/брак), `getFeed`+`weekSummary/daySummary`
(лента/печать/Excel). Мобиле (responsive `md:`-варианты) на всех полевых экранах + Сводка/План. Печать `app/print/*`.
Экспорт Excel `lib/xlsx-export`. Палитра `CULTURE_PALETTE`.

**Гарантированно работает:** свежие срезы прошли `invariant-review` (инвариантов не нарушают) + `test-runner`
(tsc/lint/build; тест-фреймворка нет — проверки ad-hoc `tsx` rolled-back + сборка).

**Не реализовано (фичи, ПОСЛЕ прода):** календарь; сравнение сезонов (нужна история, этап G); печать акта
позиции; Excel Аналитики-таблиц; снос deprecated-колонок. **Инфраструктура:** прод-выкат ещё не сделан —
приложение только в dev (Neon-БД тестовая, одноразовая). См. `PROD-DEPLOY.md`.

---

# ACTIVE RISKS

- **⚠️ Схема↔код:** `ShipmentItem.accepted_weight_kg`, `AcceptanceAct.brak_weight_kg` есть в схеме, но НЕ пишутся
  (deprecated, считаются на лету). Снос — отдельной cleanup-миграцией. Не «чинить» записью.
- **Перф `getActiveAlerts` в `(app)/layout.tsx`:** 3 доп. запроса на КАЖДУЮ навигацию сегмента (осознанно, read-only
  V1.1 без кэша). Дешёвые фиксы: guard «0 правил → skip»; агрегировать только item/location из правил; `cache()`/TTL.
- **Факт. ср. вес рейса:** частично перевешенная машина учитывается Σ только взвешенных позиций (слегка занижает →
  оценка «осталось ~N машин» консервативнее). Осознанный компромисс (DOMAIN §5).
- **Нетто-дисциплина плеч:** любое НОВОЕ плечо склада — по нетто-гарду (не по существованию), иначе повторный цикл
  после отката сломается (урок materials-fix).
- **`is_accepted` калибра — из живой схемы, не снимок акта:** правка схемы калибров задним числом меняет историю.
- **Отрицательный баланс фермера** (расход/отметка раньше прибытия) — допустим осознанно.
- **Тестовая БД:** dev/staging данные одноразовые, без data-preserving миграций до прод-вайпа.
- **`prisma migrate status`** из среды агента может не достучаться до Neon (сеть) — проверять вручную.

---

# NEXT TASK

**ПРОД-ВЫКАТ** — вывести `main` в продакшн и перейти на dev-ветку. Детали, решения и чеклист — `docs/prompts/PROD-DEPLOY.md`.
Опорные шаги (после утверждения плана):
- **Прод-БД:** отдельная от dev (Neon: prod = default-ветка `main`, dev = отдельная ветка Neon). Прод-данные
  реальны и ценны → **пересмотреть правило CLAUDE.md «dev-данные одноразовые»** (оно про dev, НЕ про прод).
- **Секреты прод:** свежий `AUTH_SECRET` (не dev-шный), прод `AUTH_URL`/домен, прод `DATABASE_URL`/`DIRECT_URL`.
- **Миграции прод:** `prisma migrate deploy` (НЕ `migrate dev`); `prisma.config.ts` для прод-миграций — через
  `DIRECT_URL` (dev-компромисс с pooled-URL был WSL-специфичным). Сид первого admin в прод.
- **Git-flow:** `main` = прод (авто-деплой), `dev` = интеграция + preview-деплой; новые фичи — ветками от `dev`.
- **Бэкапы:** проверить PITR/retention Neon-плана перед приёмом реальных данных.
- **Процесс:** это инфраструктура, не экран → без Claude Design; PM пишет `PROD-DEPLOY.md` (runbook), исполняет Claude Code + пользователь (внешние консоли Vercel/Neon).

---

# FIRST READ FOR NEXT CHAT
*(в порядке чтения; только нужное под следующий шаг)*

1. `docs/CONTEXT-HANDOFF.md` (этот файл) — состояние/решения/следующий шаг.
2. `CLAUDE.md` — стек, три базы веса, эталоны, нетто-дисциплина, правило тестовой БД.
3. `docs/prompts/_PM-WORKFLOW.md` — контур PM↔Claude Code, шаблон спеки, промпт on-demand аудитора.
4. Под рекомендованный срез — `docs/prompts/PROMPTS-ACCEPTANCE-FILTERS.md` + `PROMPTS-MOBILE-2-ACCEPTANCE.md`.
5. При задаче по логике — `docs/DOMAIN.md`: §3 (склад/движок, сентинелы −1/−2/−3, scrap-состояние, per-item
   прибытие) + BR-10 (два гейта денег), BR-20…31 (план/доска, drag-матрица, приёмка, зоны), §5 (метрики).
   Decoder-ring'и живут ТАМ — в этом handoff не дублируются. + нужный раздел `docs/PRD.md`.
6. `docs/TASKS.md` — «Бэклог пользователя» и статусы.

---

# INVARIANTS

- Всегда: балансы/стоимость/принятый вес/выполнение — считать, не хранить (одна чистая функция на формулу).
- Всегда: мутирующая server-action → `requireRole` + `$transaction` + `logChange` в той же транзакции.
- Всегда: новое плечо склада — по нетто-гарду; тара — плановый вес, ингредиент — факт, деньги — принятый.
- Всегда: состояние недели/вида/сезона — в URL; печать/экспорт — тем же чистым фильтром, что экран.
- Всегда: цвет культуры — из `Culture.color`/`CULTURE_PALETTE`; статусы отгрузок — канон `status-fills`.
- Никогда: не писать `accepted_weight_kg`/`brak_weight_kg`; не менять `prisma/schema.prisma` без обсуждения.
- Никогда: не дублировать агрегацию/формулы; не плодить вторую точку SheetJS.
- Никогда: не использовать localStorage/sessionStorage.
- Никогда: PM не редактирует прод-код; фиксы — отдельными `PROMPTS-*-FIX.md`.
- Не хранить: закешированные суммы/балансы/принятый вес.
- Использовать: существующие loaders/чистые функции (`getPlanWeek`, `getContractExecution`, `computeAcceptedKg`,
  `computeWeightedBrak`, `machineVisible`, `filterFeedWeeks`, `lib/xlsx-export`, `FilterCombo`, `PrintSheet`).

---

# OPEN QUESTIONS
*(реально открытое; принятое — не включать)*

- **ПРОД-ВЫКАТ (открыто, решается сейчас):** хостинг (Vercel managed vs on-premise/внутренняя сеть завода?);
  прод-данные (старт с чистой БД + ручной ввод справочников vs импорт из Excel?); число пользователей на старте
  (только Ivan vs операторы сразу → влияет на срочность бэкапов/RBAC-полировки). См. `PROD-DEPLOY.md`.
- **Календарь:** назначение/охват (планирование поставок по дням? связь с планом недели?) — определить дизайн-шагом.
- **Снос deprecated-колонок** `accepted_weight_kg`/`brak_weight_kg` — cleanup-миграцией; **до прод-выката** (пока
  БД можно вайпать) или отдельным прод-релизом с `migrate deploy`?
- **Сравнение сезонов** — ждёт импорта истории (этап G); проектировать сразу «на 2+ сезона» или позже?
