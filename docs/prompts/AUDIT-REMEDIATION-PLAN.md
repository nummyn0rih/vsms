# AUDIT-REMEDIATION-PLAN — разбор аудита 26.07.2026

> План закрытия находок `docs/audits/FULL-AUDIT-2026-07-26.md`. Составлен под конкретную ситуацию:
> **прод живой, пользователь ПРЯМО СЕЙЧАС наполняет базу реальными данными сезона.**
> Это меняет приоритеты относительно роадмапа аудита — расхождения объяснены ниже.

## Поправки к приоритетам аудита (и почему)

| Находка | Аудит | Здесь | Обоснование |
|---|---|---|---|
| Проверка бэкапов (роадмап п.13) | Среднесрочные (1–2 мес) | **Волна 0, сейчас** | Ты вводишь невосполнимые данные, а на free-плане Neon PITR = 6 часов и нет scheduled-снапшотов. Потерять неделю ввода реальнее и больнее, чем любая CVE в этом контуре. |
| П-12 логирование | Быстрые, п.2 | **Волна 1, первым** | В период массового ввода любой сбой сохранения сейчас невидим: пользователь видит «Не удалось», причина не логируется нигде. |
| П-4 stale JWT | High | **Понижено до Medium** (пока) | Пользователь один и он же admin — offboarding-риска физически нет. Дешёвую часть (`maxAge`) берём сразу, перечитывание роли из БД — когда появятся операторы. |
| П-1/П-2 обновления | «Сделать первым», `npm audit fix` | Волна 2, **через dev+preview со смоуком** | Согласен по важности, не согласен с порядком действий: обновление Next и auth-стека на работающем проде вслепую — сам по себе риск. Сначала dev → preview → смоук → main. |
| П-8 дубль `code` | только `Shipment` | **+ `MaterialShipment`** | Проверено: `MaterialShipment.code` (schema:372) генерится тем же `MAX+1` и тоже без `@unique`. Фикс обязан покрыть обе сущности. |

---

## Волна 0 — СЕЙЧАС, параллельно вводу данных (кода не касается) [Ivan]

Цель: сделать текущий ввод данных необратимо защищённым.

1. **Первый `pg_dump`** — сразу как введены справочники (фермеры/культуры/тара/ТК/водители/нормы):
   ```bash
   pg_dump '<прод-direct-URL>' -Fc -f vsms-prod-$(date +%F).dump
   ```
2. **Дальше — дамп в конце каждого дня ввода.** Хранить вне Neon, держать несколько поколений.
3. **Проверить восстановление** (не откладывать на «потом»): развернуть свежий дамп в проект `vsms-dev`
   и убедиться, что приложение поднимается на этих данных. Непроверенный бэкап — не бэкап.

**Критерий готовности волны:** есть дамп, который реально восстанавливался.

---

## Волна 1 — Наблюдаемость и гарды (≈1 день, БЕЗ миграций, безопасно на живом проде)

Почему первой: ничего не ломает, не трогает схему и auth, но снимает слепоту прода и закрывает
незащищённые чтения. Идёт через `dev` → preview → `main`.

| # | Находка | Что делаем | Эффект |
|---|---|---|---|
| 1.1 | **П-12** | Хелпер `failWithLog(e, msg)` с `console.error` во все `catch` в `server/**/actions.ts` (сейчас ноль логов — проверено) | Vercel-логи начинают показывать реальную причину сбоя |
| 1.2 | **П-5** | `await requireRole()` в читающие экспортируемые server actions (`getShipment`, `getTripWeightNorm`, `listShipmentOptions`) | Закрытие unguarded HTTP-эндпойнтов (defense-in-depth к П-2) |
| 1.3 | **П-18** | Удалить мёртвый `getShipments` (не используется, отдаёт все отгрузки без гарда) | Минус эндпойнт и минус мёртвый код |
| 1.4 | **П-19** | README: назначение, ссылки на `docs/`, setup (две строки Neon, seed, prod-контур) | Входная точка репо перестаёт врать |
| 1.5 | **П-7** (бонус, дёшево) | Блок `headers()` в `next.config.ts`: HSTS, X-Frame-Options, X-Content-Type-Options, базовый CSP | Гигиена заголовков |

**Спека:** `PROMPTS-AUDIT-W1-OBSERVABILITY.md` + довесок `PROMPTS-AUDIT-W1-FIX.md` (3 гарда, пропущенных спекой).
**Риск для прода:** минимальный. **Смоук:** логин → создание справочника → отгрузка → приёмка.
**Статус:** реализовано, ревью пройдено. Расхождения ревью: (1) текст `FORBIDDEN` унифицирован в «Нет прав» —
принято осознанно; (2) 3 пропущенных гарда (`listNorms`, `listOptions`, `listAlertRules`) — закрываются FIX-спекой.
⚠ **Смоук на preview перед merge в `main` обязателен** — волна переписала catch-путь во всех actions.

---

## Волна 2 — Auth-стек и CVE (окно + обязательный смоук)

Подтверждено `npm audit`: **2 critical, 8 high**. Critical — `@auth/core ≤0.41.2` (GHSA-xmf8-cvqr-rfgj и др.),
тянется через `next-auth 5.0.0-beta.31`. Отдельно — advisory Next (middleware/proxy bypass), а вся авторизация
зоны `(app)` висит именно на `proxy.ts`.

| # | Находка | Что делаем |
|---|---|---|
| 2.1 | **П-1** | Обновить `next-auth`/`@auth/core` до версии без critical-advisory |
| 2.2 | **П-2** | Обновить Next до патча из advisory (в пределах 16.x) |
| 2.3 | **П-4** (частично) | Задать `session.maxAge` разумным (напр. 8–12 ч вместо дефолтных 30 дней) |

**Статус:** реализовано (`next 16.2.12`, `next-auth 5.0.0-beta.32`, `@auth/core 0.41.3`, `maxAge` 12 ч), тесты и
смоук зелёные, включая мобильную. Смоук волны попутно вскрыл **унаследованный** дефект (роль не доезжала при
клиентской навигации) — закрыт `PROMPTS-ROLE-CONTEXT-FIX.md`. В `npm audit` осознанно остаётся только `xlsx`.

**Порядок обязателен:** ветка от `dev` → `npm audit fix` (без `--force`) → прогнать локально → preview-деплой →
смоук на preview → только потом merge в `main`.
**Обязательный смоук после обновления:** логин/логаут, роль в сессии (`/api/auth/session` → `role: admin`),
RBAC-кнопки видны, редирект неавторизованного, все 4 статуса отгрузки, сохранение акта приёмки.
**Ожидаемый побочный эффект:** смена версий auth может разлогинить — не пугаться, зайти заново.
**Спека:** `PROMPTS-AUDIT-W2-DEPS.md`. **Отложено:** перечитывание `active`/`role` из БД в jwt-callback —
когда появится второй пользователь.

---

## Волна 3 — Тестовая сетка (главный системный долг, ≈1–2 дня) — ✅ ЗАКРЫТА

Аудит прав: это долг №2 по влиянию, и он **умножает риск всех остальных правок**. Ядро (леджер, три базы
веса, нетто-идемпотентность) держится на инвариантах, которые ломаются молча. При этом всё нужное уже
чистое и тестируется без БД.

| # | Что | Итог |
|---|---|---|
| 3.1 | `vitest` + 20–30 юнит-тестов чистого ядра: `workdays.ts` (рабочие дни, UTC, ISO-недели), `calcPackagingUnits`, нетто-агрегации баланса, `calcIngredientConsumption`, `computeAcceptedKg`/`computeWeightedBrak`/`computeSettlement` (BR-33), `filterBoard`/`filterFeedWeeks` | ✅ 129 кейсов в 9 файлах `*.test.ts` рядом с модулями (+ `format.ts`, `validators.ts`, `nav.ts`). Эталон BR-33 сходится числом в число. Нетто-агрегации баланса НЕ покрыты: они не чистые (SQL-выборка в `server/inventory/`) — уходят в волну 4 вместе с SQL-агрегацией |
| 3.0 | **Задел уже есть:** `scripts/_stubs/` (подмена `@/auth` и `next/cache` через resolve-хук) — тот самый механизм, которым тестируются гардированные server actions. При переходе на vitest переиспользовать, не переписывать | ✅ Стабы не понадобились: ни один тестируемый модуль не импортирует `@/lib/prisma`, `@/auth` или `next/cache`. `scripts/_stubs/` остались как есть, для verify-скриптов |
| 3.2 | GitHub Actions: `lint + tsc + test` на push/PR в `dev` и `main` | ✅ `.github/workflows/ci.yml`. `npm run build` в CI НЕ запускается (в нём `prisma migrate deploy`), поэтому `prisma generate` — отдельным шагом с фиктивным `DIRECT_URL` |

**Почему после волн 1–2, а не до:** волна 1 безопасна и без тестов, волна 2 срочна по внешнему риску.
Дальше тесты становятся предусловием для волны 4 (там правки логики и схемы) — **предусловие снято**.
**Спека:** `PROMPTS-AUDIT-W3-TESTS.md`.

---

## Волна 4 — БД и производительность (ЕСТЬ МИГРАЦИЯ — на живом проде, data-preserving) — ✅ ЗАКРЫТА

> A (`PROMPTS-AUDIT-W4A-INDEXES.md`): 4 индекса + `@unique` на `Shipment.code` и `MaterialShipment.code` + retry
> на `P2002` + фильтр `code ~ '^[0-9]+$'` в обоих генераторах.
> B (`PROMPTS-AUDIT-W4B-BALANCES.md`): агрегация балансов через `groupBy`, гард «0 правил → к балансам не идём»,
> сужение выборки по items из правил. Parity-скрипт — 0 расхождений. Кэш сознательно не вводился.
> Единственное отличие от «байт в байт» — детерминированный порядок `cells` (теперь контракт, см. handoff).

| # | Находка | Что делаем | Нюанс |
|---|---|---|---|
| 4.1 | **П-14** | Индексы: `Shipment(arrival_date)`, `StockMovement(source_doc_type, source_doc_id)`, `ShipmentItem(shipment_id)`, `ChangeLog(entity, entity_id)` | Чисто аддитивно, данные не трогает |
| 4.2 | **П-8** | `@unique` на `Shipment.code` **и `MaterialShipment.code`** + retry на `P2002` | ⚠ **Пред-проверка:** сначала SQL на дубликаты в ПРОД-БД; при наличии — сперва починить данные, миграция иначе упадёт |
| 4.3 | **П-13** | SQL-агрегация балансов (`GROUP BY` вместо вытягивания леджера в JS) + кэш `getActiveAlerts` в layout | Самый горячий путь: агрегации на каждую навигацию |

**Обязательно:** свежий `pg_dump` ПЕРЕД миграцией (правило CLAUDE.md — прод-миграции data-preserving, деструктив
только с бэкапом). Применение — штатно `migrate deploy` на билде из `main`.
**Спеки:** `PROMPTS-AUDIT-W4-INDEXES.md` (миграция) + `PROMPTS-AUDIT-W4-BALANCES.md` (перф).

---

## Волна 5 — Приведение к собственным правилам (когда есть тесты)

| Находка | Что | Почему не раньше |
|---|---|---|
| ~~**П-11**~~ ✅ | Нетто-гард расхода ингредиентов — сделано (w5a) | — |
| ~~**П-9**~~ ✅ | Гонка авто-`accepted` — сделано (w5a): блокировка строки + идемпотентный пересчёт | — |
| ✅ **гард BR-33** | Запрет комбинации «нестандарт со строкой контракта + `settlement_percent`» — сделано (w5a) | Найдено разбором прод-данных: двойной счёт нестандарта |
| ⚠ **открыто (w5a-FIX)** | Правка `actual_weight_kg` у ПРИНЯТОЙ позиции не пересчитывает расход ингредиентов → леджер расходится с перевеской | Нетто-гард закрыл один из двух триггеров; второй (правка веса после приёмки) достижим штатным UI |
| **П-10** | Единый `todayLocalISO(tz)` с заводской таймзоной | Дёшево; проявляется только около полуночи |
| **П-20** | `logChange` — сверка результата записи | Дёшево, риск мал |
| **П-16** | Декомпозиция `BoardView`/`ShipmentsFeed` — **по правилу бойскаута**, не марафоном | Согласен с аудитом: отдельный рефакторинг-проект не окупается |
| **из w4b** | `listAlertRules`: при 0 правил не тянуть 3 справочника имён (сейчас 4 запроса на каждую навигацию независимо от наличия правил) | Имена нужны только для рендера панели дефицита. Гард по леджеру уже стоит — это остаток того же горячего пути |
| **из w4b** | Поправить комментарий `app/(app)/layout.tsx:23` — «запрос ровно один» неверно; корректно «к балансам не идём вовсе» | Формулировка вводит в заблуждение при чтении кода |
| **сверх аудита** | Вынести внутренние tx-хелперы (`applyOutboundDeliveryLeg`, `persistShipmentItems`, `loadPackagingContext`, `revertDeliveryLeg`, `persistContractLines`…) из `"use server"`-модулей в обычные server-модули | Формально это HTTP-эндпойнты; практически не вызываемы (Prisma-транзакция не сериализуется с клиента). Лечение — правка импортов, а значит риск регресса без тестов → после волны 3 |

---

## Сознательно НЕ делаем сейчас (и почему)

- **П-3 (xlsx)** — использование write-only (`lib/xlsx-export`), чужие файлы не парсятся → поверхность мала.
  Возвращаемся, когда понадобится **чтение** Excel (фича «импорт норм и контрактов» — там это станет блокером).
- **П-6 (rate limiting)** — один пользователь, непубличный URL, bcrypt(10). Вернуться при появлении операторов
  или при выносе наружу.
- **П-17 (дубли mobile/desktop)** — осознанный trade-off, зафиксирован. Не трогаем.
- **П-15 (лента по неделям)** — по аудиту «перед вторым сезоном». Не сейчас.
- **Кэш/пагинация/Docker/микросервисы** — YAGNI на 3–5 пользователей (аудит согласен).

---

## Фичи (Этап 9 аудита) — ПОСЛЕ волн 1–3

Не начинать, пока прод не стабилизирован. Топ-3 по совпадению с реальной работой:
1. **Журнал изменений** поверх ChangeLog — данные уже пишутся, но невидимы (низкая сложность, High).
2. **Расчёты с фермером** на карточке — агрегация уже есть в `execution.ts` (High).
3. **Telegram-алерты дефицита тары** — алерты сейчас видны только при заходе в систему (High).

---

## Порядок исполнения (сводка)

```
Волна 0  [Ivan, сейчас]      дамп + проверка восстановления
   ↓
Волна 1  [CC, ~1 день]       логирование · гарды чтений · README · заголовки      без миграций
   ↓
Волна 2  [CC+Ivan, окно]     next-auth · Next · maxAge                            смоук обязателен
   ↓
Волна 3  [CC, 1–2 дня]       vitest + юниты ядра · GitHub Actions                 предусловие волны 4
   ↓
Волна 4  [CC+Ivan]           индексы · @unique(2 сущности) · SQL-балансы          МИГРАЦИЯ + дамп до неё
   ↓
Волна 5  [CC]                нетто-гард · гонка accepted · today(tz) · бойскаут
```
