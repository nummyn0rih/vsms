# PROMPTS — analytics-fix: подписи брака + мёртвый fmtInt

> Самодостаточный фикс-промпт (можно запускать в НОВОМ чате Claude Code без прочего контекста).
> Дашборд `/analytics` уже реализован (Recharts + CSS-бары). Здесь — две пост-ревью правки, без новой логики.
> Без миграции, read-only, схему/данные не трогать.

## ПРОМПТ — analytics-fix (Claude Code)

```text
Задача analytics-fix (VSMS): две мелкие правки на уже готовом дашборде /analytics. Без миграции, read-only,
без изменения агрегатов/схемы. Правки строго в перечисленных файлах.

ФАЙЛЫ
- app/(app)/analytics/_components/BrakBarChart.tsx — график «% брака по культурам» (Recharts).
- lib/format.ts — есть fmtInt (строка ~13), не используется в analytics.
- app/(app)/packaging/_components/TareBalanceMatrix.tsx — содержит локальный дубль форматирования целых.

ФИКС 1 — подписи оси X в графике брака (BrakBarChart.tsx)
Сейчас названия культур вынесены в нижнюю Legend и не выровнены под столбцами. График одно-серийный
(каждый столбец = культура), поэтому:
- Показать названия культур как подписи оси X (XAxis dataKey = имя культуры), т.е. ПОД каждым столбцом.
- Цвет столбца задавать per-bar через <Cell fill=culture.color> (цвета культур — из данных, как сейчас).
- Плавающую <Legend> в этом графике УБРАТЬ (она дублирует подписи и не выровнена).
- Оси/подписи — прежние токены: hairline-сетка, Geist/Geist Mono, tabular-nums; брак — текущий янтарь
  (--an-brak #cf9a3e), не менять на статусный.
- НЕ трогать другие графики: у «Рейсы ТК» легенда остаётся (там 2 серии — овощные/материальные), у
  «Динамики» — как есть.
Сверить API XAxis/Cell Recharts через context7 (версия из package.json).

ФИКС 2 — мёртвый fmtInt (консолидация)
- Переиспользовать fmtInt из lib/format.ts вместо локального дубля в TareBalanceMatrix.tsx: удалить
  локальную функцию форматирования целых, импортировать fmtInt из '@/lib/format', заменить вызовы.
- Так fmtInt перестаёт быть мёртвым, дубль уходит. Поведение форматирования не менять.
- Если по какой-то причине сигнатуры расходятся и переиспользование меняет вывод — НЕ ломать TareBalanceMatrix;
  тогда просто удалить неиспользуемый fmtInt из lib/format.ts и сообщить об этом.

ОГРАНИЧЕНИЯ
- Только эти два файла (+ возможно lib/format.ts). Без миграции, без новых зависимостей, read-only.
- Не менять агрегаты server/analytics, другие графики, токены.

ПРОВЕРКА (показать)
- npm run lint + npx tsc --noEmit + npm run build — зелёные.
- График «% брака»: названия культур стоят под своими столбцами, цвет столбца = культура, нижней легенды нет.
- «Рейсы ТК» и «Динамика» — без изменений.
- fmtInt: либо переиспользован в TareBalanceMatrix (дубль удалён), либо удалён из format.ts; мёртвого кода нет.
В конце предложи git-коммит одной строкой (напр. `fix(analytics): подписи оси X в графике брака + консолидация fmtInt`).
```

### Чеклист ревью analytics-fix
1. BrakBarChart: культуры — подписи XAxis под столбцами; per-bar цвет через `<Cell>`; Legend убрана; токены/янтарь не тронуты.
2. Другие графики (динамика, рейсы ТК) не изменены; легенда ТК на месте.
3. `fmtInt` переиспользован в TareBalanceMatrix (дубль удалён) ИЛИ удалён из format.ts — мёртвого кода нет.
4. Без миграции/новых зависимостей; read-only; lint/tsc/build зелёные.
