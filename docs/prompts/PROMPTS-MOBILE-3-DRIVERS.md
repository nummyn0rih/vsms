# PROMPTS — mobile-3: контакты водителей на мобиле

> Третий (последний) срез мобильной версии v1. **Responsive**, тот же код/данные; на узком экране (<md) —
> карточки водителей с крупной tel:-кнопкой + поиск/ТК-фильтр. Десктоп по поведению НЕ менять. **Без миграции,
> read-only.** Прототип: `mobile-v1.html` (экран 3). Шелл/таб-бар из mobile-1 — переиспользовать.

## Решения (зафиксированы)
| Развилка | Выбор |
|---|---|
| Раскладка | Карточный список водителей: аватар-инициалы, ФИО, ТК, **крупная tel:-кнопка (≥44px)**, мессенджер/инфо если есть. |
| Фильтры | Поиск по фамилии (full-width) + фильтр по ТК (компактный / bottom-sheet). Логика — существующая (`CompanyFilter`, URL-параметры), не дублировать. |
| Данные | Тот же список водителей (server/drivers) + `formatPhone`/`normalizePhone` из `lib/validators`. |
| Десктоп | `DriversTable` (md+) без изменений. Read-only, правка водителей — десктоп. |

---

## ПРОМПТ — mobile-3 (Claude Code)

```text
Задача mobile-3 (VSMS): мобильная раскладка контактов водителей (<md) — карточки + tel:. Responsive, те же
данные/фильтры. Десктоп по поведению НЕ менять. Миграция: НЕТ. Read-only.

Перед кодом прочитать:
- docs/prototypes/mobile-v1.html — экран 3 (контакты водителей: поиск, ТК-фильтр, карточки с call-кнопкой). Источник вёрстки.
- app/(app)/reference/drivers/page.tsx, _components/DriversTable.tsx, CompanyFilter.tsx — десктоп-список,
  фильтр по компании + поиск по фамилии (в URL). Переиспользовать данные/фильтры.
- lib/validators.ts — formatPhone (+7 XXX XXX-XX-XX) / normalizePhone (для tel:). Телефон кликабельный.
- server/drivers/* — загрузка списка (ФИО, phone, transport_company, info).
- DESIGN-SYSTEM.md — токены; mobile-1 шелл (app-bar/таб-бар) — переиспользовать.

UI (мобиле, <md)
1) Новый MobileDriversList (client, `md:hidden`), десктоп DriversTable — `hidden md:block`. Данные — те же
   (из page.tsx). Sticky-строка сверху: поиск по фамилии (input, ≥16px) + ТК-фильтр (компактный combobox или
   bottom-sheet) — на СУЩЕСТВУЮЩЕЙ логике фильтра (URL-параметры компании/поиска, как десктоп; не дублировать).
2) Карточка водителя: аватар-инициалы, ФИО, ТК (+ мессенджер/инфо если есть), телефон (formatPhone) и
   **крупная tel:-кнопка** (`href="tel:"+normalizePhone`, ≥44px). Опц. группировка по ТК (заголовки) — как в прототипе.
3) Пустое состояние (нет водителей / пусто-после-фильтра) — аккуратно, по стилю мобильных экранов.

ОГРАНИЧЕНИЯ
- Без миграции/схемы. Read-only (просмотр + звонок; правка — десктоп). Данные/фильтры — существующие, не
  дублировать. Десктоп DriversTable/фильтр не трогать по поведению. Состояние поиска/ТК — как сейчас (URL).
- Тач-цели ≥44px; поле поиска ≥16px; токены DESIGN-SYSTEM. localStorage нельзя. Доки/TASKS — PM.

БД ТЕСТОВАЯ: UI — визуальная проверка на узком/широком вьюпорте.

ПРОВЕРКА (показать)
- Узкий экран: карточки водителей; поиск по фамилии и ТК-фильтр сужают список; tel:-кнопка звонит (formatPhone
  отображение, normalizePhone в href); пусто-после-фильтра.
- Широкий экран (md+): десктоп-таблица водителей и её фильтр БЕЗ изменений (регресс).
- Read-only: на мобиле нет создания/правки водителя.
В конце предложи git-коммит одной строкой.
```

### Чеклист ревью mobile-3
1. Responsive: <md — MobileDriversList (карточки + tel:); md+ — DriversTable без регресса.
2. Данные/фильтры — существующие (список + CompanyFilter/поиск по URL), без дублей; телефон formatPhone/normalizePhone.
3. Тач ≥44px; поиск ≥16px; токены из системы; пустые состояния.
4. Без миграции; read-only; десктоп не тронут; localStorage не используется.

---

## После задачи — обновление памяти (зона PM, не Claude Code)
- TASKS.md: mobile-3 → `[x]`; **мобильная версия v1 ЗАКРЫТА** (3/3 среза).
- CONTEXT-HANDOFF.md: PROJECT STATE / CURRENT IMPLEMENTATION += мобильная версия v1 (шелл/лента/приёмка/водители);
  CURRENT OBJECTIVE/NEXT TASK → следующий F/V2 (печатные формы PDF / сравнение сезонов после истории) или бэклог.
- Отметить: мобильный responsive-паттерн (шелл + `md:`-варианты + карточные списки) — эталон для будущих мобильных экранов.
