# PROMPTS — filters-materials (V1.1): фильтры на «Логистику материалов»

> Последний срез V1.1 (по согласованному объёму — только `/materials`). Добавить фильтры
> **Фермер · Вид · Статус** на /materials по эталону тулбара «Ленты»; вынести общий `FilterCombo` в
> shared-локацию и переиспользовать (лента + /materials), без регресса ленты. **Без миграции.**
> Read-only (фильтр — это вид). Остальные экраны (дашборды тары/ингредиентов, справочники) — бэклог.

## Решения (зафиксированы с пользователем)
| Развилка | Выбор |
|---|---|
| Объём | Только `/materials`. Дашборды/справочники — бэклог. |
| Оси фильтра | Фермер (получатель позиции, searchable мультивыбор) · Вид (тара/ингредиент) · Статус (planned/sent/arrived + производный «частично»). **НЕ культура** — у материалов её нет. |
| Реализация | Общий компонент: вынести `FilterCombo` из `shipments/_components` в shared, переиспользовать в ленте и /materials. |
| Состояние фильтров | Клиентское (Set, как в ленте). Неделя остаётся в URL (не трогать). localStorage не использовать. |
| Уровень фильтрации | По рейсу: рейс виден, если есть подходящая позиция И статус подходит; подытоги пересчитываются из видимого набора. |

---

## ПРОМПТ — filters-materials (Claude Code)

```text
Задача filters-materials (VSMS): добавить фильтры (Фермер · Вид · Статус) на /materials по эталону тулбара
«Ленты», вынеся общий FilterCombo в shared. Миграция: НЕТ. Read-only. Не дублировать логику — переиспользовать.

Перед кодом прочитать:
- app/(app)/shipments/_components/FilterCombo.tsx — эталонный combobox-фильтр (generic: ComboOption,
  selected Set, onToggle/onClear, kind "icon"|"status", searchable). Его выносим в shared.
- app/(app)/shipments/_components/ShipmentsFeed.tsx — эталон ПРИМЕНЕНИЯ фильтров (тулбар, клиентское
  состояние Set, live-apply, «Сбросить», пусто-после-фильтра, пересчёт подытогов из видимого набора).
- server/materials/feed-loader.ts — типы MaterialTrip/item: code, status, derivedStatus (planned/sent/
  arrived + «частично» производный), items[].farmerId/farmerName, items[].itemKind (packaging|ingredient).
- app/(app)/materials/_components/MaterialsFeed.tsx — структура/рендер, куда встаёт тулбар; итоги (тара шт /
  ингредиент Σ кг/л).
- server/materials/actions.ts listMaterialOptions — фермеры/предметы для опций комбо (если удобнее, чем из feed).
- docs/DESIGN.md (паттерн фильтров ленты), CLAUDE.md (неделя/вид — в URL; фильтры ленты — клиентские).

ШАГИ
1) Вынести FilterCombo в общую локацию (напр. components/filters/FilterCombo.tsx или components/ui/), обновить
   импорт в ленте. Поведение ленты НЕ менять (регресс-проверка). Классы/стили combo не ломать (они глобальные).
2) /materials — тулбар с тремя FilterCombo:
   - Фермер (kind="icon", searchable): опции — фермеры-получатели из feed/listMaterialOptions.
   - Вид (kind="icon" или "status"): тара / ингредиент.
   - Статус (kind="status"): Плановая / Отправлена / Прибыла / Частично (производный derivedStatus).
   + «Сбросить» (сброс всех). Поиск (код рейса / фермер) — опционально, как в ленте.
3) Фильтрация на уровне РЕЙСА (чистая функция, переиспользуемая): рейс виден если
   (нет выбранных фермеров ИЛИ ∃ позиция с farmerId ∈ выбранных) И
   (нет выбранных видов ИЛИ ∃ позиция с itemKind ∈ выбранных) И
   (нет выбранных статусов ИЛИ derivedStatus ∈ выбранных).
   Подытоги (тара шт / ингредиент Σ по ед.) пересчитать из ВИДИМЫХ рейсов теми же хелперами, что и сейчас.
   Пусто-после-фильтра — состояние как в ленте.
4) Состояние фильтров — клиентское (useState<Set>), как в ленте. Неделя — в URL (?week), НЕ трогать.
   localStorage не использовать.

ОГРАНИЧЕНИЯ
- Без миграции/схемы. Read-only — фильтр это вид, никаких мутаций/server-actions на запись.
- Не дублировать: FilterCombo — один общий; функция фильтрации/пересчёта — чистая, переиспользуемая.
- Токены/классы фильтра — из эталона ленты (нейтральная заливка active + счётчик, не статусные цвета).
- Доки/TASKS — PM.

БД ТЕСТОВАЯ: проверки seed/rolled-back (здесь в основном UI — визуальная проверка + чистые функции).

ПРОВЕРКА (показать)
- Фильтр по фермеру / виду / статусу по отдельности и в комбинации → корректный видимый набор рейсов.
- Производный статус «частично» фильтруется наравне с остальными.
- Подытоги (тара/ингредиент) пересчитываются из видимого набора.
- «Сбросить» очищает все; пусто-после-фильтра показывает состояние.
- Лента НЕ регрессировала: её фильтры Поставщик/Сырьё/Статус работают как раньше (общий FilterCombo).
- RBAC: доступ как у /materials; мутаций нет.
В конце предложи git-коммит одной строкой.
```

### Чеклист ревью filters-materials
1. `FilterCombo` вынесен в shared, лента импортирует оттуда, её фильтры не регрессировали.
2. /materials: три фильтра (Фермер searchable / Вид / Статус вкл. «частично»), клиентское состояние, «Сбросить».
3. Фильтрация на уровне рейса — чистая функция; подытоги пересчитаны из видимого; пусто-после-фильтра.
4. Неделя в URL не тронута; localStorage не используется; классы/токены combo из эталона.
5. Без миграции; read-only; логика не продублирована.

---

## После задачи — обновление памяти (зона PM, не Claude Code)
- TASKS.md: «V1.1 · фильтры везде» → `[x]` (объём: /materials; дашборды/справочники — бэклог).
- CONTEXT-HANDOFF.md: PROJECT STATE / CURRENT IMPLEMENTATION += фильтры /materials; **V1.1 закрыт**.
  Обновить CURRENT OBJECTIVE/NEXT TASK: V1.1 завершён → предложить ретро/следующий блок.
- Бэклог: фильтры на дашбордах тары/ингредиентов и простых справочниках; PDF акта; аналитика брака; мобильная.
- Следующий крупный блок — этап F/V2 (сравнение сезонов, графики Recharts, печатные формы) ИЛИ добор V1.1-бэклога.
