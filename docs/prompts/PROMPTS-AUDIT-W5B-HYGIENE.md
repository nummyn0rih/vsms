# PROMPTS — audit-w5b: гигиена (П-10, П-20 + остатки w4b)

> Волна 5, часть B — последняя по плану аудита. Мелкие, независимые, низкорисковые правки.
> **Миграции НЕТ. Бизнес-логику и расчёты не трогаем.**

## Что закрываем

| # | Находка | Где | Суть |
|---|---|---|---|
| 1 | **П-10** | 5 мест | «Сегодня» считается в UTC → для UTC+3 с 00:00 до 03:00 сервер думает, что ещё вчера |
| 2 | **П-20** | `server/changelog.ts` | `createMany` без сверки результата, хотя CLAUDE.md требует «проверять результат строками в БД» |
| 3 | из w4b | `server/alert-rules/actions.ts` | `listAlertRules` тянет 3 справочника имён даже когда правил ноль |
| 4 | из w4b | `app/(app)/layout.tsx:23` | Комментарий «запрос ровно один» фактически неверен |

**Снято с плана (проверено, проблемы нет):** «вынести tx-хелперы из `"use server"`-модулей». `cultures/calibre.ts`,
`shipments/items.ts`, `shipments/packaging.ts`, `materials/movements.ts`, `contracts/lines.ts` — уже обычные
серверные модули; `"use server"` встречается в них только в поясняющих комментариях. Пункт был заведён по ошибке
(`grep -rl` матчил комментарии).

## Решение по таймзоне

Единая константа заводской таймзоны в одном месте, по умолчанию **`Europe/Moscow`**. ⚠ Значение подтвердить
у пользователя перед мержем — если завод в другой зоне, меняется одна строка.

---

## ПРОМПТ — audit-w5b (Claude Code)

```text
Задача audit-w5b (VSMS): гигиена по итогам аудита — локальное «сегодня», сверка записи ChangeLog, экономия
запросов в алертах, неверный комментарий. Миграция: НЕТ. Расчёты и бизнес-логику НЕ менять.

Перед кодом прочитать: server/shipments/workdays.ts (parseDateUTC, UTC-дисциплина дат),
server/changelog.ts, server/alert-rules/actions.ts (listAlertRules), server/alert-rules/alerts.ts,
app/(app)/layout.tsx, CLAUDE.md (правило 6 — проверять результат строками в БД; UTC-дисциплина).

1) П-10 — единое локальное «сегодня»:
   - В server/shipments/workdays.ts добавить экспорт FACTORY_TZ = "Europe/Moscow" (одна константа, с комментарием
     «заводская таймзона; менять здесь») и чистую функцию todayLocalISO(tz = FACTORY_TZ): string —
     сегодняшняя дата в этой зоне в формате YYYY-MM-DD (через Intl.DateTimeFormat с timeZone, БЕЗ ручной
     арифметики часов).
   - Заменить на неё все вызовы `new Date().toISOString().slice(0, 10)`:
     server/shipments/actions.ts:244 · server/acceptance/accepted.ts:181 · server/acceptance/actions.ts:108 ·
     app/(app)/planner/_components/BoardView.tsx:86 · app/(app)/planner/_components/WholeMachineDialog.tsx:43.
     ⚠ Клиентские два — тоже: иначе браузер и сервер разойдутся в понятии «сегодня».
   - ⚠ НЕ трогать parseDateUTC и хранение дат: даты В БД остаются UTC-полуночью. Меняется только вычисление
     «какое сегодня число», не разбор и не запись.
   - Юнит-тесты в workdays.test.ts: todayLocalISO для фиксированного момента (замороженное время) даёт
     локальную дату; кейс 00:30 по местному времени (когда UTC ещё вчера) — ключевой.

2) П-20 — сверка записи ChangeLog (server/changelog.ts):
   - createMany возвращает { count }: сверить с list.length; при несовпадении — бросить ошибку
     (мы всегда внутри транзакции вызывающего → изменение откатится вместе с логом).
   - Пустой список: сейчас молча `return`. Оставить return, но добавить console.warn с указанием вызывающего
     контекста — молчаливый пустой диф маскирует забытое заполнение entries.

3) Остаток w4b — listAlertRules (server/alert-rules/actions.ts):
   - Сначала читать правила; если их НЕТ — вернуть пустой результат, НЕ выполняя три запроса справочников
     (packagingType/ingredient/farmer): имена нужны только для рендера панели дефицита.
   - Форму возврата не менять; поведение при наличии правил — прежнее.

4) app/(app)/layout.tsx:23 — поправить комментарий: не «без правил дефицита запрос ровно один», а
   «без правил дефицита к балансам не идём вовсе» (по факту listAlertRules делал 4 запроса; после п.3 — 1).
   Комментарий привести в соответствие с фактическим поведением после правки.

ОГРАНИЧЕНИЯ
- Не менять формулы, схему, RBAC, движения склада.
- Не менять формат хранения дат (UTC) и parseDateUTC.
- Не вводить библиотеки дат (dayjs/luxon) — достаточно Intl.
- FACTORY_TZ — ровно одна константа, без дублей и без чтения из env.

ПРОВЕРКА (показать)
- todayLocalISO: тест с замороженным временем 2026-08-01T22:30:00Z → при Europe/Moscow ожидаем 2026-08-02
  (а старый код дал бы 2026-08-01). Все 5 мест используют хелпер, прямых toISOString().slice(0,10) не осталось
  (показать грепом).
- ChangeLog: искусственно рассинхронить (например, временно подсунуть дубль-нарушение) → операция падает,
  а не пишет тихо меньше строк. Временную правку откатить.
- Алерты: при 0 правил в БД — ровно 1 запрос (сами правила), справочники не читаются; при наличии правил
  панели дефицита не изменились.
- npm run test зелёный (включая новые кейсы); lint/tsc/build зелёные.
В конце предложи git-коммит одной строкой.
```

### Чеклист ревью audit-w5b
1. `FACTORY_TZ` — одна константа; `todayLocalISO` чистая, на `Intl`, без ручной арифметики.
2. Все 5 мест переведены (включая два клиентских); прямых `toISOString().slice(0,10)` не осталось.
3. Даты в БД по-прежнему UTC; `parseDateUTC` не тронут.
4. `logChange` сверяет `count` и падает при расхождении; пустой диф логируется предупреждением.
5. `listAlertRules` при 0 правил делает 1 запрос; форма ответа прежняя.
6. Комментарий в `layout.tsx` соответствует фактическому поведению.
7. Тесты (включая кейс «полночь по местному») + lint/tsc/build зелёные.

---

## После задачи — обновление памяти (зона PM)
- `AUDIT-REMEDIATION-PLAN.md`: волна 5 закрыта целиком → **план аудита пройден**; снять пункт про вынос
  tx-хелперов как ошибочно заведённый.
- `CONTEXT-HANDOFF.md`: в ARCHITECTURAL DECISIONS — «„сегодня“ считается в заводской таймзоне (`FACTORY_TZ`),
  хранение дат остаётся UTC»; снять П-10/П-20 из рисков.
- `CLAUDE.md`: в конвенции — «для „сегодня“ использовать `todayLocalISO()`, не `new Date().toISOString()`».
