# PROMPTS — export-xlsx-fix: кнопку «Экспорт Excel» в row 1 к «Печать»

> Самодостаточный фикс-промпт (запускать в НОВОМ чате Claude Code без прочего контекста).
> export-xlsx реализован. Кнопка «Экспорт Excel» ленты сейчас в СТРОКЕ ФИЛЬТРОВ (row 2, внутри `{showFilters
> && ...}`), а «Печать»/«Развернуть все» — в row 1. Рассогласование + та же уязвимость, что уже ловили с печатью
> (кнопка в row 2 исчезает при `showFilters={false}`). Перенести `exportSlot` в row 1 рядом с `printSlot`.
> **Без миграции, read-only, поведение не меняется — только место кнопки.**

## ПРОМПТ — export-xlsx-fix (Claude Code)

```text
Задача export-xlsx-fix (VSMS): перенести кнопку «Экспорт Excel» ленты из строки фильтров (row 2) в row 1 рядом
с «Печать». Миграция: НЕТ. Read-only. Логику экспорта не менять — только место рендера кнопки в тулбаре.

ФАЙЛ
- components/shell/FeedToolbar.tsx — Row 1 (`<div className="tbar-row">`, ~стр. 106) содержит createSlot / неделю /
  вид / `{expandSlot}` / `{printSlot}` (~стр. 144–145). Row 2 (`{showFilters && (<div className="tbar-row">...)`,
  ~стр. 171) содержит поиск/фильтры и `{exportSlot}` (~стр. 230).

ФИКС
- Перенести `{exportSlot}` из row 2 (из блока `{showFilters && ...}`) в row 1 — сразу после `{printSlot}` (стр. ~145),
  чтобы «Печать» и «Экспорт Excel» стояли рядом в верхней всегда-видимой строке.
- Убедиться: exportSlot больше не рендерится внутри `{showFilters && ...}`; на Ленте кнопка одна (не задвоилась);
  порядок в row 1 логичный (напр.: … Развернуть все · Печать · Экспорт Excel). Отступы/выравнивание — как у соседних.
- Приёмка (её кнопки «Печать»/«Экспорт») — уже рядом, не трогать, если не требуется симметрии.

ОГРАНИЧЕНИЯ
- Только FeedToolbar.tsx (место рендера). Логику xlsx-экспорта/сбор данных/фильтры НЕ менять. Без миграции,
  read-only, без новых зависимостей.

ПРОВЕРКА (показать)
- Лента: «Экспорт Excel» стоит в верхней строке рядом с «Печать»; экспорт по-прежнему работает (тот же файл/данные).
- Кнопка не задвоилась; в строке фильтров её больше нет.
- Виды «Сводка»/«План» (showFilters=false) не сломаны (у них своя «Печать» в row 1; Excel-кнопки там нет — как и было).
- lint/tsc/build зелёные.
В конце предложи git-коммит одной строкой (напр. `fix(shipments): кнопка «Экспорт Excel» в row 1 рядом с «Печать»`).
```

### Чеклист ревью export-xlsx-fix
1. `exportSlot` рендерится в row 1 рядом с `printSlot`, вне блока `{showFilters && ...}`; на Ленте одна кнопка.
2. Логика экспорта не тронута; данные/файл те же.
3. Сводка/План не сломаны; без миграции; lint/tsc/build зелёные.
