# PROMPTS — summary (Сводка/Heatmap): heatmap текущей недели на /shipments

> Закрыть плейсхолдер «Сводка скоро» на `/shipments?view=summary`. Вид «Сводка» = read-only heatmap текущей
> недели: культуры × рабочие дни, ячейка = эффективный вес (факт ?? план) Σ по поставщикам; справа итоги по
> культуре (план · факт · % · Δ). **Без миграции. Без новой агрегации — переиспользуем `getPlanWeek`.**
> Прототип утверждён: `docs/prototypes/summary-v1.html`.

## Решения (зафиксированы с пользователем)
| Развилка | Выбор |
|---|---|
| Охват | Текущая неделя из URL `?week` (как «План»), ось дни × культуры. Смена недели → навигация `?week=` (server re-fetch), НЕ scroll как в «Ленте». |
| Метрика ячейки | Эффективный вес `actual_weight_kg ?? planned_weight_kg` (BR-22), Σ по всем поставщикам за день по `arrival_date`. |
| Итоги по культуре | План = `weekTarget ?? Σ dayTargets`; Факт = Σ эфф. за неделю; % = факт/план; Δ = факт − план. План отсутствует/0 → %/Δ = «—». |
| Интенсивность заливки | **По строке (внутри культуры):** opacity ∝ значение / макс. ячейка этой культуры за неделю, мин. видимый порог ~18%. Цвет — `Culture.color`. Нулевая ячейка — без заливки. |
| Состав | Heatmap + зона итогов (4 колонки равной ширины). week-mode культура — без дневных ячеек, факт только в итогах, бейдж «неделя» (BR-23). |
| Доступ | Read-only, все роли (R). Никаких мутаций/StockMovement. |

---

## ПРОМПТ — summary (Claude Code)

```text
Задача summary (VSMS): реализовать вид «Сводка» на /shipments — read-only heatmap текущей недели
(культуры × рабочие дни, ячейка = эффективный вес Σ по поставщикам) + итоги по культуре. Миграция: НЕТ.
Новой server-агрегации НЕ писать — данные берём из существующего getPlanWeek.

Перед кодом прочитать:
- docs/prototypes/summary-v1.html — вёрстка, токены, структура (источник вида).
- server/plan/board.ts — getPlanWeek: возвращает days (рабочие дни недели), rows[culture]
  { cultureId, cultureName, color, mode: "day"|"week", weekTarget, dayTargets, dayProgress: Record<date,
  {actualTons, planRemainingTons, effectiveTons}>, weekProgress }, dayTotalsProgress[], weekTotalProgress.
- server/plan/schema.ts — типы PlanWeek/PlanRow/CellProgress.
- server/shipments/workdays.ts — parseWeekParam, isoWeekRange, workdaysOfWeek (неделя из URL).
- app/(app)/shipments/page.tsx — текущая загрузка (getFeed + listShipmentOptions), parseWeekParam, ?view.
- app/(app)/shipments/_components/ShipmentsFeed.tsx — переключатель видов (Лента|Сводка) и плейсхолдер
  при view==="summary" (его заменить); как неделя/вид пишутся в URL.
- DOMAIN.md BR-22 (эффективный вес), BR-23 (видимость строки, week-mode культура), DESIGN-SYSTEM.md (токены).
Next/Prisma — context7 при необходимости.

SERVER
1) Данные вида «Сводка» — ТОЛЬКО из getPlanWeek({ seasonYear, isoYear, isoWeek }) по неделе из URL
   (parseWeekParam). НЕ писать новую агрегацию, НЕ дублировать суммирование. В page.tsx для вида summary
   догрузить getPlanWeek параллельно (или дочерним server-loader) и передать PlanWeek в клиентский вид.
   - Неделя «Сводки» = ?week (дефолт текущая). Никаких мутаций, ничего не хранить. RBAC — существующего
     layout-гарда достаточно (вид read-only для всех ролей).

UI
2) Новый клиентский компонент SummaryView — заменить плейсхолдер «Сводка скоро» в ShipmentsFeed при
   view==="summary". По summary-v1.html:
   - Таблица культура × рабочий день (колонки = PlanWeek.days). Ячейка = rows[c].dayProgress[date]?.effectiveTons
     (нет данных → 0, рисовать как «·» без заливки).
   - Интенсивность заливки ПО СТРОКЕ: для каждой культуры rowMax = max(effectiveTons по её дневным ячейкам);
     opacity% = rowMax>0 ? round(18 + 82 * value/rowMax) : 0; заливка = color-mix(in srgb, rows[c].color
     opacity%, transparent). Нулевая ячейка — без заливки. (Текст числа белый на насыщенных ячейках —
     порог по читаемости, как в прототипе.)
   - Зона итогов по культуре, 4 колонки РАВНОЙ ширины: План (weekTarget ?? Σ dayTargets), Факт
     (weekProgress.effectiveTons), % (полоса: заполнение = min(100, факт/план*100); перелёт >100 — акцент
     var(--pos), полная полоса), Δ = факт−план (знак, цвет pos/neg). План 0/нет → %/Δ = «—», полосу не рисовать.
   - week-mode культура (rows[c].mode==="week"): дневные ячейки не показывать (объединённая ячейка-заглушка
     на ширину дней + бейдж «неделя»), факт только в зоне итогов (BR-23).
   - Подвал «Итого по дням»: по колонкам дней = dayTotalsProgress[i].effectiveTons (loader уже исключает
     week-mode из дневных, BR-23); колонка «Факт» подвала = weekTotalProgress.effectiveTons (все культуры).
   - Headline в контекст-полосе «Выполнение недели X / Y т · %»: Σ effective(культур с целью) / Σ целей
     (BR-22) — только по культурам, у которых есть план.
   - Форматирование тонн: 1 знак, целые — целыми; tnum + Geist Mono для чисел; токены DESIGN-SYSTEM
     (radius, hairline, без статус-цветов отгрузок). № недели — Geist Mono.
3) Переключатель видов Лента|Сводка уже есть. Убедиться: переход на «Сводка» показывает данные недели из URL;
   стрелки ‹/› и «Сегодня» меняют ?week и перегружают данные (server re-fetch), а не скроллят как «Лента».

ОГРАНИЧЕНИЯ
- Без миграции/схемы. Read-only — никаких server-actions, StockMovement, записи accepted_weight_kg/brak_weight_kg.
- Агрегацию НЕ дублировать — единственный источник чисел getPlanWeek (один источник истины с видом «План»).
- localStorage/sessionStorage нельзя — неделя/вид только в URL. Логика — в server-loader; компонент рендерит.
- Доки/TASKS/прототип не трогать (обновит PM).

БД ТЕСТОВАЯ: проверки seed/rolled-back.

ПРОВЕРКА (показать)
- Неделя с дневными целями: ячейки культур×дни заполнены эфф. весом; насыщенность по строке (макс. день
  культуры — самый тёмный); слабая культура (перец) читается так же, как томаты.
- week-mode культура: дневных ячеек нет, бейдж «неделя», факт виден в итогах; в подвале «Итого по дням»
  она НЕ участвует, а в колонке «Факт» подвала — участвует.
- Культура без цели, но с отгрузками за неделю: строка видна (BR-23), План пуст → %/Δ = «—».
- Смена недели (‹/›/Сегодня) → данные перегружаются по ?week; вид «Сводка» сохраняется.
- Пустая неделя → пустое состояние.
- RBAC: user видит вид (read-only); мутаций нет.
- Сверка чисел: Факт культуры = Σ её дневных ячеек (day-mode); подвал день = Σ day-mode культур;
  колонка «Факт» подвала = grand total (вкл. week-mode). % и Δ согласованы с «Планом» (тот же getPlanWeek).
В конце предложи git-коммит одной строкой.
```

### Чеклист ревью summary
1. Источник данных — только `getPlanWeek`; новой агрегации/дублей суммирования нет.
2. Ячейка = `effectiveTons`; интенсивность по строке (rowMax), порог ~18%, цвет из `Culture.color`.
3. Итоги: 4 колонки равной ширины; План = `weekTarget ?? Σ dayTargets`; %/Δ скрыты при отсутствии плана; полоса % заполняется (не inline-span с width — `display:block`).
4. week-mode культура и подвал — по BR-23 (день-подытоги без week-mode; «Факт» подвала — все).
5. Неделя/вид в URL; смена недели = re-fetch, не scroll. Read-only, без мутаций, без миграции, без localStorage.

---

## Фикс шапки (summary-fix, после первой реализации)
Симптом: sticky-`thead` «Сводки» всплывает посреди таблицы и просвечивает. Две причины в `app/globals.css`
(сверка с рабочей матрицей «Плана» `table.pmatrix`, у которой бага нет):
1. **`.hm-wrap { overflow-x: auto }`** → по CSS `overflow-y: visible` при этом вычисляется в `auto`, обёртка
   становится вертикальным скролл-контейнером, sticky-шапка прилипает к ней, а не к странице. **Фикс:**
   `.hm-wrap { overflow: visible }` (как `.matrixwrap` у «Плана»; один скролл — страницы).
2. **`table.hm { border-collapse: collapse }`** ломает sticky-`th` + низкий `z-index`. **Фикс:**
   `border-collapse: separate; border-spacing: 0`; поднять z-index под паттерн `pmatrix`:
   `.hm thead th` z-index `2 → 20`; `.hm thead th.cor` `4 → 30`; `.hm td.cult` (tbody sticky-колонка) `3 → 10`.
Только CSS, без изменения TSX/логики. Проверка: шапка закреплена под контекст-полосой при вертикальном скролле,
непрозрачна, горизонтальный скролл узкой таблицы работает через страницу.

## После задачи — обновление памяти (зона PM, не Claude Code)
- TASKS.md: V1.1 «Сводка/Heatmap» → `[x]`.
- CONTEXT-HANDOFF.md: PROJECT STATE / CURRENT IMPLEMENTATION += вид «Сводка»; снять из OPEN QUESTIONS
  «приоритет V1.1» (следующий — AlertRule-алерты по согласованному порядку).
- PRD.md: отметить «Экран 5 — Heatmap» реализованным как вид «Сводка» на /shipments (день×культура, эфф. вес).
- Следующий срез V1.1 по порядку пользователя: AlertRule-алерты → карточка поставщика → фильтры.
